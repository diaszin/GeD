services:
  db:
    image: postgres:latest
    container_name:  ged-db
    ports: 
    - "${DB_PORT}:5432"
    env_file:
      - .env

    networks:
      - ged-network

networks:
  ged-network:
    external: true
    name: ged-network

